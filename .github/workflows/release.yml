name: Verified release

on:
  push:
    tags:
      - "v*"
      - "[0-9]*"

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    runs-on: windows-2022

    steps:
      - name: Check out tagged source
        uses: actions/checkout@v6

      - name: Configure MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Build Release Win32
        run: msbuild BorderlessMode.sln /t:Rebuild /p:Configuration=Release /p:Platform=Win32

      - name: Package release
        id: package
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}" -replace '^v', ''
          if ($version -notmatch '^\d+\.\d+\.\d+$') {
              throw "Release tag must contain a semantic version."
          }
          $archiveName = "BorderlessMode-v$version.zip"
          $archive = Join-Path build $archiveName
          $checksum = "$archive.sha256"
          $stage = Join-Path build "release\BorderlessMode-v$version"
          Remove-Item -LiteralPath $stage -Recurse -Force -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Path $stage -Force | Out-Null
          Copy-Item build\BorderlessMode.asi, Config\BorderlessMode.ini -Destination $stage
          Copy-Item README.md -Destination (Join-Path $stage README.txt)
          Compress-Archive -Path "$stage\*" -DestinationPath $archive -Force
          $hash = (Get-FileHash -Algorithm SHA256 -LiteralPath $archive).Hash.ToLowerInvariant()
          "$hash  $archiveName" | Set-Content -LiteralPath $checksum -Encoding ascii

          $changelog = Get-Content CHANGELOG.md -Raw
          $pattern = "(?ms)^##\s+v?$([regex]::Escape($version))(?:\s+-[^\r\n]+)?\s*\r?\n(?<body>.*?)(?=^##\s|\z)"
          $match = [regex]::Match($changelog, $pattern)
          if (-not $match.Success) {
              throw "CHANGELOG.md has no section for $version."
          }
          $notes = @(
              $match.Groups['body'].Value.Trim()
              ""
              "### Verifying the download"
              ""
              "The archive was built by GitHub Actions from tag `${{ github.ref_name }}`. Compare it with the published SHA-256 file or verify its signed build provenance:"
              ""
              '```text'
              "gh attestation verify $archiveName -R $env:GITHUB_REPOSITORY"
              '```'
          ) -join [Environment]::NewLine
          [IO.File]::WriteAllText(
              (Join-Path build release-notes.md),
              $notes,
              [Text.UTF8Encoding]::new($false)
          )
          "version=$version" >> $env:GITHUB_OUTPUT
          "archive=$archive" >> $env:GITHUB_OUTPUT
          "checksum=$checksum" >> $env:GITHUB_OUTPUT

      - name: Attest release archive
        uses: actions/attest@v4
        with:
          subject-path: ${{ steps.package.outputs.archive }}

      - name: Publish GitHub release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ github.ref_name }}" `
            "${{ steps.package.outputs.archive }}" `
            "${{ steps.package.outputs.checksum }}" `
            --verify-tag `
            --latest `
            --title "BorderlessMode v${{ steps.package.outputs.version }}" `
            --notes-file build\release-notes.md
